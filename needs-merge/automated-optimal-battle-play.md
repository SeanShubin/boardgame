# Automated optimal (perfect) battle play — why it's tractable — ✅ MERGED

> **MERGED into canon 2026-06-26 by session `perfect solver`.** This theory record (PvE is single-agent ⇒
> pure optimal play; luck-off = reachability, luck-on = expectimax; the blind bid is benign; minimax only
> in PvP) folded into **`docs/games/deckbound/computability-and-balance.md` §10.7** alongside its build
> plan (`perfect-solver-plan.md`).
>
> Original promotion target was `docs/game-theory/`, but that directory is game-agnostic
> (counter-system theory: RPS, Pokémon); this content is deckbound-specific (Spec §0.x, creatures, the
> blind bid, `deckbound::solver`) and is inseparable from the build plan — so it lives with the plan in
> the §10 runbook instead. **The canonical source is §10.7 — edit there, not here.**
